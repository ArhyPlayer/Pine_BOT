# 🤖 Диалоговый бот с памятью

> 📸 **Демонстрация работы:** с примерами работы бота можно ознакомиться в директории [Screenshots](Screenshots).

Умный персональный ассистент с долговременной памятью на базе Pinecone, Haystack и OpenAI. Помнит всё, что вы пишете, анализирует загруженные документы (PDF, DOCX, PPTX и другие) через Docling и использует всю накопленную информацию для точных персонализированных ответов.

## 🚀 Быстрый старт

### Шаг 1: Установка зависимостей
```bash
python -m venv .venv
source .venv/bin/activate  # macOS/Linux
# или .venv\Scripts\activate на Windows

pip install -r requirements.txt
```

### Шаг 2: Настройка .env
```bash
cp env-example .env
```

Откройте `.env` и заполните:
- `TELEGRAM_BOT_TOKEN` - получите у [@BotFather](https://t.me/botfather)
- `OPENAI_API_KEY` - ваш ключ OpenAI API
- `PINECONE_API_KEY` - ваш ключ Pinecone API
- `PINECONE_INDEX_NAME` - имя индекса (например, "dialog-bot")

### Шаг 3: Создание индекса Pinecone (опционально)
Индекс создастся автоматически при первом запуске, или создайте вручную:
- Dimension: **1536**
- Metric: **cosine**
- Cloud: **AWS**
- Region: **us-east-1**

### Шаг 4: Запуск бота
```bash
python bot.py
```

### Шаг 5: Тестирование
Откройте вашего бота в Telegram и отправьте `/start`

**Готово! 🎉** Бот запущен и готов к работе!

---

## 🌟 Особенности

- **🧠 Долговременная память**: каждое ваше сообщение сохраняется в Pinecone и используется в будущих ответах
- **📄 Анализ документов**: отправьте PDF, DOCX, PPTX или HTML — бот изучит содержимое и будет отвечать на вопросы по нему
- **🎯 Единый контекст**: при ответе агент одновременно учитывает ваши сообщения и загруженные документы
- **🔍 Семантический поиск**: находит релевантную информацию по смыслу, а не по ключевым словам
- **🚫 Защита от дубликатов**: проверяет косинусное сходство, обновляет похожие записи
- **📋 Авто-резюме**: после загрузки файла бот сразу даёт краткое описание его содержимого
- **🗑️ Управление памятью**: просмотр сообщений и загруженных документов, полная очистка
- **🤖 Haystack-агент с инструментами**: при установленных зависимостях работает как агент с тулзами: факты о собаках, фото породы с описанием через OpenAI Vision

## 📋 Требования

- Python 3.8+
- Telegram Bot Token (от [@BotFather](https://t.me/botfather))
- OpenAI API Key
- Pinecone API Key

## 🔧 Подробная настройка

### Настройка переменных окружения

Отредактируйте `.env` файл со следующими параметрами:

```env
# Токен Telegram-бота (получить у @BotFather)
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# API ключ OpenAI
OPENAI_API_KEY=your_openai_api_key_here

# Base URL для ProxyAPI (опционально)
OPENAI_BASE_URL=https://api.proxyapi.ru/openai/v1

# Ключ доступа к Pinecone
PINECONE_API_KEY=your_pinecone_api_key_here

# Имя индекса Pinecone
PINECONE_INDEX_NAME=dialog-bot

# Модель для эмбеддингов
EMBEDDING_MODEL=text-embedding-3-small

# Модель чата
CHAT_MODEL=o4-mini-2025-04-16
```

### Создание Telegram бота

1. Откройте [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env` файл

## 📱 Команды бота

- `/start` — Приветствие и краткое описание возможностей
- `/help` — Справка: команды, поддерживаемые форматы файлов, примеры
- `/memory` — Посмотреть загруженные документы и запомненные сообщения
- `/clear` — Полностью очистить память (сообщения + документы)
- `/forget [текст]` — Найти в памяти похожую информацию

> 📄 **Для загрузки файла** просто отправьте его боту как вложение — никаких команд не нужно.

## 💡 Примеры использования

### Обычный диалог — бот запоминает сообщения
```
👤 Привет! Я люблю программировать на Python и работаю разработчиком
🤖 Привет! Здорово — запомнил, что ты разработчик на Python!

👤 Мой любимый цвет — синий
🤖 Отлично, учту это.

[Позже]
👤 Какой дизайн лучше выбрать?
🤖 Может быть, что-то в синих тонах? Ведь это твой любимый цвет...
```

### Загрузка документа
```
👤 [отправляет report.pdf]
🤖 📄 Файл получен. Запускаю анализ и сохранение.
   Это может занять немного времени…

   ✅ Готово. Я изучил этот файл, теперь можем его обсудить.
   📋 Документ содержит финансовый отчёт компании за 2024 год
      с разбивкой доходов по кварталам и регионам.
```

### Вопросы по загруженному документу
```
👤 Какой квартал был наиболее прибыльным?
🤖 Согласно отчёту, наиболее прибыльным стал третий квартал —
   выручка составила 4,2 млрд рублей, что на 18% выше Q2.

👤 Расскажи подробнее про регион Сибирь
🤖 В документе указано, что сибирское направление показало рост 23%...
```

### Просмотр памяти с документами
```
👤 /memory
🤖 🧠 Моя память о тебе:

📊 Всего записей: 58

📁 Загруженные документы:
  1. report.pdf (54 фрагмента)

💬 Запомненные сообщения:
  1. Привет! Я люблю программировать на Python...
  2. Мой любимый цвет — синий
  3. Какой квартал был наиболее прибыльным?

💡 /clear — очистить всю память
```

### Совместный контекст: документ + личные данные
```
👤 Подготовь для меня краткую сводку по отчёту в стиле для разработчика
🤖 Вижу, что ты разработчик — вот техническая сводка:
   • API-интеграции: выросли на 34%
   • Инфраструктурные расходы: снизились на 12%...
```

### Запрос всего, что бот знает
```
👤 Что ты знаешь обо мне?
🤖 По моим записям:
   - Ты разработчик, любишь Python
   - Любимый цвет — синий
   - Загружен документ: report.pdf (финансовый отчёт 2024)
```

## 🤖 Haystack-агент

Бот интегрирован с фреймворком [Haystack](https://haystack.deepset.ai/). При наличии зависимостей каждое сообщение обрабатывает полноценный агент с инструментами (tools), а не просто запрос к OpenAI.

### Как проверить, что агент активен

При запуске `bot.py` в консоли должна появиться строка:
```
✓ Haystack-агент инициализирован
```

Если написано `⚠️ haystack-ai не установлен` — установите зависимости:
```bash
pip install -r requirements.txt
```

### Как работает агент

```
Сообщение пользователя
        │
        ▼
 MemoryManager.retrieve()          ← семантический поиск по Pinecone
        │ контекст (факты)
        ▼
  HaystackAgent.reply()
        │
        ▼
 OpenAIChatGenerator (o4-mini)     ← LLM с системным промптом + контекстом
        │ нужен инструмент?
    ┌───┴───────────────────┐
    │                       │
    ▼                       ▼
dog_fact              dog_image_describe
(внешний API)         (внешний API + Vision)
    │                       │
    └───────┬───────────────┘
            │ результат инструмента
            ▼
   Финальный ответ пользователю
        │
        ▼
 MemoryManager.extract_facts()     ← извлечение фактов
 MemoryManager.save()              ← сохранение в Pinecone
```

### Инструменты агента

#### 🐶 `dog_fact` — случайный факт о собаках
Агент вызывает инструмент автоматически, когда понимает, что пользователь просит факт о собаках.

**Фразы-триггеры:**
```
Расскажи интересный факт о собаках
Что-нибудь необычное про собак?
Знаешь что-то интересное о собаках?
```

**Пример диалога:**
```
👤 Расскажи факт о собаках
🤖 Конечно! 🐶 Собаки — единственные животные, которые могут понимать
   указательный жест человека так же, как это делают дети.
```

---

#### 📸 `dog_image_describe` — фото собаки + описание породы через OpenAI Vision
Агент получает случайное фото с [dog.ceo](https://dog.ceo), отправляет его в `gpt-4o-mini` Vision и возвращает определение породы с кратким описанием её происхождения.

**Фразы-триггеры:**
```
Покажи картинку собаки
Хочу увидеть какую-нибудь собаку
Покажи собаку и расскажи о породе
Какая это порода?
```

**Пример диалога:**
```
👤 Покажи какую-нибудь собаку
🤖 Вот фото: https://images.dog.ceo/breeds/husky/...

   Это Сибирский хаски. Порода была выведена народом чукчей на
   северо-востоке Сибири для упряжной езды. Хаски отличаются
   выносливостью, дружелюбным характером и могут преодолевать
   огромные расстояния в суровых условиях. В начале XX века
   порода попала на Аляску и прославилась на гонках собачьих упряжек.
```

### Как добавить новый инструмент

1. Создайте Haystack-компонент в `agent/tools.py`:

```python
@component
class MyTool:
    """Описание для LLM: когда использовать этот инструмент."""

    @component.output_types(result=str)
    def run(self, **kwargs) -> dict:
        # логика инструмента
        return {"result": "ответ"}
```

2. Зарегистрируйте его в `agent/assistant.py` в методе `_build()`:

```python
my_tool = ComponentTool(
    component=MyTool(),
    name="my_tool",
    description="Краткое описание для LLM: когда вызывать.",
)
return Agent(
    ...
    tools=[dog_fact_tool, dog_image_tool, my_tool],
    ...
)
```

3. Добавьте упоминание инструмента в `_SYSTEM_PROMPT` класса `HaystackAgent`.

---

## 📄 Обработка документов (Docling)

Бот умеет принимать файлы прямо в Telegram и сохранять их содержимое в Pinecone для дальнейшего анализа.

### Поддерживаемые форматы

| Расширение | Описание |
|------------|----------|
| `.pdf`     | PDF-документы с текстом и таблицами |
| `.docx`    | Документы Microsoft Word |
| `.pptx`    | Презентации PowerPoint |
| `.html`, `.htm` | HTML-страницы |
| `.md`      | Markdown-файлы |
| `.adoc`, `.asciidoc` | AsciiDoc-файлы |

### Как загрузить файл

Просто отправьте файл боту как вложение — никаких команд не нужно:

```
1. Откройте диалог с ботом в Telegram
2. Нажмите значок скрепки / прикрепить файл
3. Выберите документ на устройстве
4. Отправьте
```

Бот ответит немедленно, а обработка идёт в фоне:

```
📄 Файл получен. Запускаю анализ и сохранение.
   Это может занять немного времени…

✅ Готово. Я изучил этот файл, теперь можем его обсудить.
📋 <Одно предложение — краткое резюме содержимого>
```

### Что происходит под капотом

```
Файл из Telegram
       │
       ▼
  Docling.convert()          ← разбор PDF/DOCX/HTML, таблиц, заголовков
       │ DocumentChunker
       ▼
  Чанки с метаданными        ← filename, page_no, headings
       │
       ▼
  PineconeManager.upsert()   ← каждый чанк → эмбеддинг → Pinecone
       │
       ▼
  MemoryManager.save_doc_index()  ← «паспорт» документа (имя, кол-во чанков)
       │
       ▼
  OpenAI (резюме)            ← бот отправляет пользователю 1 предложение
```

### Вопросы по документу после загрузки

После загрузки любого файла просто задавайте вопросы в свободной форме:

```
👤 Кратко суммируй основные тезисы
👤 Что написано про раздел 3?
👤 Есть ли в документе упоминание [ключевого слова]?
👤 Сравни данные из первой и последней главы
```

Агент автоматически ищет релевантные фрагменты в Pinecone и формирует ответ.

### Просмотр загруженных документов

Команда `/memory` покажет список всех проиндексированных файлов:

```
📁 Загруженные документы:
  1. report.pdf (54 фрагмента)
  2. contract.docx (12 фрагментов)
```

### Повторная загрузка файла

Если вы загрузите файл с тем же именем ещё раз, запись в индексе обновится (старые чанки не накапливаются).

### Включение Docling

Если при запуске бот сообщает `⚠️ docling не установлен — загрузка файлов недоступна`, выполните:

```bash
pip install docling
```

Установка занимает несколько минут (пакет тянет тяжёлые зависимости для ML-анализа документов).

---

## 🏗️ Архитектура

### Структура проекта
```
Pine_BOT/
├── bot.py                  # Точка входа: сборка зависимостей, запуск polling
├── config.py               # Класс Config — настройки из .env и константы
├── memory/                 # Работа с Pinecone и долговременной памятью
│   ├── store.py            # PineconeManager — низкоуровневые операции с индексом
│   └── manager.py          # MemoryManager — поиск, сохранение, форматирование контекста
├── agent/                  # Haystack-агент с инструментами
│   ├── assistant.py        # HaystackAgent — сборка и запуск агента
│   └── tools.py            # DogFactTool, DogImageDescribeTool (@component)
├── documents/              # Обработка документов через Docling
│   ├── ingestion.py        # DoclingIngestionPipeline — разбор, чанкинг, сохранение
│   └── __init__.py         # Экспорт: DoclingIngestionPipeline, SUPPORTED_EXTENSIONS
├── handlers/               # Telegram-обработчики
│   └── telegram.py         # BotHandlers — команды, текст и загрузка файлов
├── examples/               # Примеры использования отдельных модулей
│   └── pinecone_usage.py
├── requirements.txt        # Зависимости проекта
├── .env                    # Переменные окружения (не в git)
├── env-example             # Пример .env
├── .gitignore
├── LICENSE
└── README.md
```

### Алгоритм работы

**Текстовое сообщение:**

1. Сообщение пользователя поступает в `BotHandlers._on_text`
2. `MemoryManager.retrieve()` ищет семантически близкие фрагменты в Pinecone (сообщения + документы)
3. Контекст + история сессии + сообщение передаются в `HaystackAgent.reply()`
4. Агент при необходимости вызывает инструменты (dog_fact, dog_image_describe)
5. Ответ отправляется пользователю
6. Сообщение пользователя сохраняется в Pinecone (`type="message"`)

**Загрузка файла:**

1. Файл поступает в `BotHandlers._on_document`, бот немедленно отвечает «получен»
2. В фоновом потоке запускается `DoclingIngestionPipeline.process()`
3. Docling разбирает документ, `DocumentChunker` делит на фрагменты
4. Каждый чанк сохраняется в Pinecone (`type="doc_chunk"`) с метаданными
5. Сохраняется «паспорт» документа (`type="doc_index"`)
6. `DoclingIngestionPipeline.summarize()` генерирует однострочное резюме
7. Бот отправляет пользователю «Готово» + резюме

### Система памяти

Pinecone хранит три типа записей:

| Тип | Что хранит | Кто создаёт |
|-----|-----------|-------------|
| `message` | Сырой текст сообщения пользователя | `BotHandlers._on_text` |
| `doc_chunk` | Фрагмент документа + метаданные (filename, page_no, headings) | `DoclingIngestionPipeline.process` |
| `doc_index` | «Паспорт» документа (имя файла, количество фрагментов) | `DoclingIngestionPipeline.process` |

**Особенности:**
- Каждый тип записи преобразуется в эмбеддинг (1536 измерений, `text-embedding-3-small`)
- Поиск по семантическому сходству (cosine similarity)
- Защита от дубликатов для сообщений: похожие записи обновляются, а не дублируются
- Изолированный `namespace` для каждого пользователя (`user_{user_id}`)
- При ответе `MemoryManager.format_for_context()` разделяет результаты на два блока: «Из загруженных документов» и «Прошлые сообщения пользователя»

**Что игнорируется:**
- Общие вопросы без информации
- Команды боту
- Благодарности и приветствия

**Пример:**
```
Вход: "Я люблю программировать на Python и работаю в IT"
Извлечено: 
  - Увлекается программированием на Python
  - Работает в IT сфере
```

### Настройки дубликатов

В `memory/store.py` можно настроить пороги сходства:

```python
SIMILARITY_THRESHOLD_HIGH = 0.80  # Выше - дубликат (обновляем)
SIMILARITY_THRESHOLD_LOW = 0.75   # Ниже - новая информация (сохраняем)
```

**Статусы сходства:**
- **score ≥ 0.85** → "duplicate" - обновляет существующий факт
- **0.7 ≤ score < 0.85** → "similar" - сохраняет как новый факт
- **score < 0.7** → "new" - явно новая информация

### Специальные запросы

При определенных запросах бот возвращает **все факты без фильтрации** по релевантности:

**Триггерные фразы:**
- "все предпочтения"
- "мои предпочтения"
- "напомни мне"
- "расскажи о мне"
- "что ты знаешь обо мне"

Это полезно для команды `/memory` и запросов типа "напомни все, что знаешь".

## 🔧 Настройка и оптимизация

### Изменение модели чата

Отредактируйте параметр `CHAT_MODEL` в `.env`:
```env
CHAT_MODEL=o4-mini-2025-04-16  # Или другая модель
```

### Изменение модели эмбеддингов

При изменении модели эмбеддингов нужно также обновить размерность в индексе:
```env
EMBEDDING_MODEL=text-embedding-3-large  # Dimension: 3072
```

### Настройка параметров памяти

В файле `handlers/telegram.py` можно изменить:
- `top_k` — количество фактов для извлечения (по умолчанию 10)

В файле `agent/assistant.py` в `generation_kwargs`:
- `max_completion_tokens` — максимальная длина ответа агента (по умолчанию 1000)

В файле `memory/store.py` настройки порогов дубликатов:
- `SIMILARITY_THRESHOLD_HIGH = 0.80` — выше считается дубликатом (обновляется)
- `SIMILARITY_THRESHOLD_LOW = 0.75` — ниже считается новой информацией

**Примечание:** Модель `o4-mini-2025-04-16` не поддерживает настройку `temperature` - используется только значение по умолчанию.

## 🐛 Решение проблем

### Ошибка "Невозможно разрешить импорт"
```bash
# Убедитесь, что виртуальное окружение активировано
source .venv/bin/activate  # macOS/Linux
.venv\Scripts\activate     # Windows

# Переустановите зависимости
pip install -r requirements.txt
```

### Ошибка подключения к Pinecone
- Проверьте правильность `PINECONE_API_KEY`
- Убедитесь, что индекс создан с правильной размерностью (1536)
- Проверьте интернет-соединение

### Ошибка OpenAI API
- Проверьте наличие средств на аккаунте OpenAI
- Убедитесь в правильности `OPENAI_API_KEY`
- Если используете ProxyAPI, проверьте `OPENAI_BASE_URL`

### Бот не отвечает в Telegram
- Убедитесь, что бот запущен (`python bot.py`)
- Проверьте правильность `TELEGRAM_BOT_TOKEN`
- Убедитесь, что бот не заблокирован в Telegram

### Загрузка файлов не работает
Если в консоли при запуске:
```
⚠️ docling не установлен — загрузка файлов недоступна
```
Установите пакет:
```bash
pip install docling
```
Установка занимает несколько минут — Docling подтягивает ML-зависимости для анализа документов.

### Файл загружается, но бот отвечает «Формат не поддерживается»
Убедитесь, что расширение файла входит в список поддерживаемых:
`.pdf`, `.docx`, `.pptx`, `.html`, `.htm`, `.md`, `.adoc`, `.asciidoc`

### Обработка файла зависает или занимает очень долго
Docling выполняет глубокий ML-анализ документа (OCR, таблицы, разметка). Для больших PDF (50+ страниц) это нормально. Бот продолжает отвечать на другие сообщения — обработка идёт в фоне.

## 📊 Мониторинг

### Логи

Бот использует `loguru` — логи выводятся с цветом и уровнями:

```
INFO     | Запуск бота (модель: o4-mini-2025-04-16, индекс: dialog-bot)
SUCCESS  | ✓ Haystack-агент инициализирован
SUCCESS  | ✓ Docling ingestion pipeline инициализирован
DEBUG    | Поиск памяти для user_id=123, query='Напомни предпочтения...'
DEBUG    | Найдено 3 фрагментов: [message] Я люблю Python... (0.71), [doc_chunk] report.pdf p.2 (0.65)
INFO     | [user=123] Сообщение сохранено в Pinecone
INFO     | [user=123] Начало обработки файла: report.pdf
SUCCESS  | [user=123] Файл report.pdf обработан: 54 чанка сохранено
WARNING  | ⚠️ Обнаружен дубликат (score=0.91) — обновляем запись
ERROR    | Ошибка при обработке файла report.pdf: ...
```

### Статистика Pinecone
Проверить количество записей в индексе:
```python
from memory.store import PineconeManager
pm = PineconeManager()
stats = pm.get_stats()
print(stats)
```

## 🔐 Безопасность

- **Не публикуйте** файл `.env` в публичных репозиториях
- Используйте `.gitignore` для исключения `.env`
- Регулярно обновляйте API ключи
- Ограничьте доступ к Pinecone индексу

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. Подробности в файле [LICENSE](LICENSE).

## 🤝 Поддержка

При возникновении вопросов или проблем:
1. Проверьте раздел "Решение проблем"
2. Убедитесь, что все зависимости установлены
3. Проверьте правильность настроек в `.env`

## 🚀 Реализованные возможности

- ✅ Долговременная память: сохранение сообщений пользователя в Pinecone
- ✅ Обработка документов через Docling (PDF, DOCX, PPTX, HTML, MD)
- ✅ Семантический поиск по сообщениям и документам одновременно
- ✅ Автоматическое резюме после загрузки файла
- ✅ Персональные namespace для каждого пользователя
- ✅ Защита от дубликатов (cosine similarity)
- ✅ Haystack-агент с инструментами (dog fact, dog image + Vision)
- ✅ Краткосрочная история сессии для диалогового контекста
- ✅ Фоновая обработка файлов (бот не блокируется)
- ✅ Логирование через loguru

## 💡 Возможные улучшения

- [ ] Поддержка голосовых сообщений
- [ ] Экспорт памяти в JSON
- [ ] Выборочное удаление конкретного документа
- [ ] Напоминания на основе сохраненной информации
- [ ] Web-интерфейс для визуализации памяти
- [ ] Поддержка таблиц из документов в ответах

---

Создано с ❤️ для персонализированного общения
